.leftcol
  %h3 Create a New User
  - form_for @user, :url => users_path, :html => {:method => :post, :multipart => true} do |f|
    = render_error_msg(@user)
    = render :partial => 'form', :locals => {:f => f, :user => current_user}
    .formfield
      = f.submit "Create User"
