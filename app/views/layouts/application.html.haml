!!!XML
!!! 5
%html
  %head
  %title Treasures of Quilting - Custom, Picture, Memory and T-Shirt Quilts and Quality Quilted Products
  %meta{"http-equiv"=>"Content-Type", :content =>"text/html; charset=utf-8"}
  = stylesheet_link_tag 'base', 'gallery'
  = javascript_include_tag 'prototype', 'effects','application', 'slideshow', 'gallery'
  %body
  %section#header
    %header
      #logo.shadow.hidden.floatLeft
        %hgroup
          %h1= link_to "Treasures of Quilting", "/"
          %h2 Custom, Picture, Memory and T-Shirt Quilts and Quality Quilted Products.
      %nav
        = render :partial => 'layouts/navigation', :locals => {:object => @webpage.nil? ? @subpage : @webpage}
  %section#content.clearfix
    .inner
      = render :partial => 'shared/website_sidebar', :locals => {:webpages => @webpages}
      = yield
  %footer
    %nav
      = render :partial => 'layouts/navigation', :locals => {:object => @webpage.nil? ? @subpage : @webpage}
